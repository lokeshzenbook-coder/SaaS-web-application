# name: CD - Build & Publish

# on:
#   push:
#     branches: [main]
#     paths:
#       - "src/**"
#       - "public/**"
#       - "package.json"
#       - "package-lock.json"
#       - "Dockerfile"
#       - ".dockerignore"

# concurrency:
#   group: deploy-${{ github.ref }}
#   cancel-in-progress: false

# permissions:
#   contents: read
#   packages: write
#   id-token: write # required for build attestations

# env:
#   REGISTRY: ghcr.io

# jobs:
#   build-and-push:
#     name: Build, scan & push image
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Image name (lowercase repo)
#         id: image
#         run: |
#           IMAGE=${{ github.repository }}
#           echo "name=${IMAGE@L}" >> "$GITHUB_OUTPUT"
#           echo "IMAGE_NAME=${IMAGE@L}" >> "$GITHUB_ENV"

#       - name: Log in to GHCR (OIDC, no PAT needed)
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Build image for scanning (no push)
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           push: false
#           load: true
#           tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:probe
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#       - name: Scan image with Trivy (fail on HIGH/CRITICAL)
#         uses: aquasecurity/trivy-action@v0.36.0
#         with:
#           image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:probe
#           format: sarif
#           output: trivy-results.sarif
#           exit-code: "1"
#           ignore-unfixed: true
#           severity: HIGH,CRITICAL

#       - name: Upload Trivy report
#         if: always()
#         uses: github/codeql-action/upload-sarif@v3
#         with:
#           sarif_file: trivy-results.sarif
#           category: trivy

#       - name: Build and push image to GHCR
#         id: build
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           push: true
#           tags: |
#             ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
#             ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:main
#             ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           provenance: false

#       - name: Attach build provenance (SLSA)
#         uses: actions/attest-build-provenance@v2
#         with:
#           subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
#           subject-digest: ${{ steps.build.outputs.digest }}
#           push-to-registry: true

#   deploy:
#     name: Deploy
#     needs: build-and-push
#     runs-on: ubuntu-latest
#     if: github.ref == 'refs/heads/main'
#     env:
#       DEPLOY_TARGET: ${{ secrets.DEPLOY_TARGET }}
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       # This job is a no-op until you set a DEPLOY_TARGET secret.
#       # Example for a VPS over SSH (set DEPLOY_TARGET, DEPLOY_HOST, DEPLOY_USER,
#       # DEPLOY_SSH_KEY, and APP_PORT in repo secrets, then uncomment below):
#       #
#       # - name: Deploy over SSH
#       #   if: env.DEPLOY_TARGET != ''
#       #   uses: appleboy/ssh-action@v1.2.0
#       #   with:
#       #     host: ${{ secrets.DEPLOY_HOST }}
#       #     username: ${{ secrets.DEPLOY_USER }}
#       #     key: ${{ secrets.DEPLOY_SSH_KEY }}
#       #     script: |
#       #       docker pull ghcr.io/lokeshzenbook-coder/saas-web-application:main
#       #       docker stop saas-web || true
#       #       docker rm saas-web || true
#       #       docker run -d --name saas-web --restart unless-stopped \
#       #         -p ${{ secrets.APP_PORT }}:3000 \
#       #         -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
#       #         -v saas-data:/app/data \
#       #         ghcr.io/lokeshzenbook-coder/saas-web-application:main

#       - name: Skipping deploy (no DEPLOY_TARGET configured)
#         if: env.DEPLOY_TARGET == ''
#         run: echo "Set the DEPLOY_TARGET secret to enable deployment."
